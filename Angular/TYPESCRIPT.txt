Son los componentes y archivos .ts

1) se hacen los imports

2) decorador @Component({})

3) export class NameComponent{

    //propiedades
    tipoDeAcceso nombre: tipoDeDato;
    public titulo: string;

    //constructor
    constructor( private _servicio : provider){
        //dar valor a las propiedades
        this.titulo = "Hola Mundo";
    }

    metodos(valorIngresante<tipoDeDatoIngresante>): tipoDeDatoDevuelto{
        accion;
    }
}